module p_count (
		input wire clk,
		input wire rst,
		input wire [31:0] n_pc;
		output reg [31:0] pc;
		);

   always @(posedge clk) begin
      if (rst)
	pc <= 0;
      else
	pc <= n_pc;
   end
endmodule // p_count
