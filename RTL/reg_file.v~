module reg_file (
		 input wire clk,
		 input wire we,
		 input wire [4:0] rs1,
	         input wire [4:0] rs2,
		 input wire [4:0] rd,
		 input wire [31:0] wd,
		 output reg [31:0] rd1,
		 output reg [31:0] rd2
		 );
   always @(posedge clk) begin
      if (rst)
	rd1 <= 0;
        rd2 <= 0;
      else
	if 
	
